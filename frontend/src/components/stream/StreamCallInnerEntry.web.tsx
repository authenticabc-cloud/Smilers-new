/**
 * StreamCallInnerEntry (WEB) — Stream Video SDK is native-only; render nothing
 * on web (CallHost already returns null on web anyway).
 */
export default function StreamCallInnerEntryWeb() {
  return null;
}
