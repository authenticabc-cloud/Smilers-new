{
  "cli": {
    "appVersionSource": "local"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "android": {
        "buildType": "apk",
        "gradleCommand": ":app:assembleDebug"
      },
      "env": {
        "EXPO_NO_DOTENV": "1"
      }
    },
    "preview": {
      "distribution": "internal"
    },
    "production": {
      "autoIncrement": true
    },
    "app-bundle": {
      "autoIncrement": true,
      "android": {
        "buildType": "app-bundle"
      }
    }
  },
  "submit": {
    "production": {
      "android": {
        "//note": "FCM v1 service account JSON is referenced here so EAS automatically uploads it as the FCM v1 push credential for project com.smilers.app. The same file is what Expo's push server uses when forwarding pushes to Firebase Cloud Messaging. Generated from Firebase Console → Project Settings → Service Accounts → Generate new private key for project smilers-a4e07.",
        "serviceAccountKeyPath": "./google-service-account.json"
      }
    }
  }
}
